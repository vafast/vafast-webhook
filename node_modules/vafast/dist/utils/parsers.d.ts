interface FileInfo {
    name: string;
    type: string;
    size: number;
    data: ArrayBuffer;
}
interface FormData {
    fields: Record<string, string>;
    files: Record<string, FileInfo>;
}
/**
 * 简化的请求体解析函数
 * 优先简洁性，处理最常见的场景
 */
declare function parseBody(req: Request): Promise<unknown>;
/**
 * 解析请求体为特定类型
 * 提供类型安全的解析方法
 */
declare function parseBodyAs<T>(req: Request): Promise<T>;
/**
 * 解析请求体为表单数据
 * 专门用于处理 multipart/form-data
 */
declare function parseFormData(req: Request): Promise<FormData>;
/**
 * 解析请求体为文件
 * 专门用于处理文件上传
 */
declare function parseFile(req: Request): Promise<FileInfo>;
/** 获取查询字符串，直接返回对象 */
declare function parseQuery(req: Request): Record<string, unknown>;
/**
 * 快速解析简单查询字符串（不支持嵌套，但更快）
 * 适用于简单的 key=value&key2=value2 场景
 */
declare function parseQueryFast(req: Request): Record<string, string>;
/** 解析请求头，返回对象 */
declare function parseHeaders(req: Request): Record<string, string>;
/**
 * 获取单个请求头（避免解析全部）
 */
declare function getHeader(req: Request, name: string): string | null;
/** 使用cookie库解析Cookie，保证可靠性 */
declare function parseCookies(req: Request): Record<string, string>;
/**
 * 快速解析 Cookie（简化版，不使用外部库）
 * 适用于简单的 cookie 场景
 */
declare function parseCookiesFast(req: Request): Record<string, string>;
/**
 * 获取单个 Cookie 值（避免解析全部）
 */
declare function getCookie(req: Request, name: string): string | null;

export { type FileInfo, type FormData, getCookie, getHeader, parseBody, parseBodyAs, parseCookies, parseCookiesFast, parseFile, parseFormData, parseHeaders, parseQuery, parseQueryFast };
